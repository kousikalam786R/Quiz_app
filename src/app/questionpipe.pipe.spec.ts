import { QuestionpipePipe } from './questionpipe.pipe';

describe('QuestionpipePipe', () => {
  it('create an instance', () => {
    const pipe = new QuestionpipePipe();
    expect(pipe).toBeTruthy();
  });
});
